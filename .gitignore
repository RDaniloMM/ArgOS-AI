# Rust build artifacts
/target

# ArgOS runtime data directory (wiki source of truth + regenerable derived indexes)
.argos/

# SQLite database files (runtime, regenerable from .argos/wiki/)
*.db
*.db-journal
*.db-wal
*.db-shm

# sqlite-vec index databases (regenerable via `argos reindex`)
*.index.db

# Gentle AI skill registry
.atl/

# Node.js dependencies (Playwright E2E tests)
node_modules/

# Codex OAuth E2E — token cache (sensitive, regenerable)
tests/codex-e2e/.last-token*

# Graphify output (knowledge graph)
graphify-out/
