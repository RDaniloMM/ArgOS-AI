//! ArgOS n8n connector.
//!
//! Bidirectional MCP integration with n8n (the workflow engine). ArgOS discovers,
//! imports, exports, executes (by delegation), and monitors n8n workflows. MCP is
//! the primary transport; REST is the fallback. Implementation lands in later tasks.
